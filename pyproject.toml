[project]
name = "asteroids"
version = "2.3.0"
description = "a game like the original Asteroids but with a few twists"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pygame==2.6.1",
]
